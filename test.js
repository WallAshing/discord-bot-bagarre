var text = ["début", " première suite", "le contenu", " encore du contenu", " toujours du contenue"];

var newText = text.slice(2)

var TotuLeContenu = "";

for (let i = 0; i < newText.length; i++) {
    TotuLeContenu += newText[i]
}

console.log(TotuLeContenu)

